
// This component has been replaced by the Auth page using Supabase authentication
// The file is kept for compatibility but should be removed in future cleanup
import React from 'react';

const LoginForm = () => {
  return null;
};

export default LoginForm;
